@{
    // Apply different layouts based on user role
    if (User.Identity.IsAuthenticated && User.IsInRole("Admin"))
    {
        Layout = "_AdminLayout";
    }
    else
    {
        Layout = "_CustomerLayout";
    }
}
